# Copy to .env on the GPU host. Never commit the real .env file.
MIRA_API_KEY=replace-with-a-long-random-secret
MIRA_FRONTEND_ORIGIN=https://your-mira-domain.example
MODEL_ID=Wan-AI/Wan2.2-I2V-A14B-Diffusers
MODEL_CACHE_DIR=/models
OUTPUT_DIR=/data/outputs
DATA_ROOT=/data
MAX_QUEUE_SIZE=20
MAX_FRAMES=64
MIN_VRAM_GB=80
DEFAULT_STEPS=8
DEFAULT_FPS=16
DEFAULT_WIDTH=832
DEFAULT_HEIGHT=480
JOB_RETENTION_HOURS=24
LOAD_MODEL_ON_STARTUP=false
HF_TOKEN=
